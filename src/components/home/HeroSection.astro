---
import Button from "#components/Button.svelte";
import Surface from "#components/Surface.astro";
import Region from "../Region.astro";
---

<Region top="3xl" bottom="2xl">
  <div class="hero">
    <div class="description">
      <Surface background="yellow-2" text="black" rotate={-1.8} border>
        <h2 class="text-2xl">Le mag des mordus de sports&nbsp;!</h2>
        <p>
          Parce que le sport mérite d’avoir son magazine jeunesse et que la
          presse jeunesse mérite d’avoir son magazine de sport, nous avons
          imaginé À fond ! Un magazine imprimé de 60 pages, pour toutes les
          petites filles et les petits garçons de 7 à 11 ans.
        </p>
      </Surface>
      <div class="btns">
        <Button href="/boutique">S'abonner</Button>
        <Button href="https://www.calameo.com/read/0071538196f9628abe6c3"
          >Feuilleter</Button
        >
      </div>
    </div>

    <img
      src="/mojoacro.png"
      alt=""
      style="margin-inline:auto;"
      slot="sidebar"
    />
  </div>
</Region>

<style>
  .hero {
    display: grid;
    gap: var(--space-xl);
    & .description {
      display: flex;
      flex-direction: column;
      gap: var(--space-m);
    }

    .btns {
      display: flex;
      justify-content: center;
      font-size: var(--step-1);
      gap: var(--space-s);
    }
    img {
      max-height: 40vmin;
    }

    @media (width>60rem) {
      align-items: center;
      gap: var(--space-xl);
      grid-template-columns: minmax(30rem, 2fr) 1fr;
      & .btns {
        justify-content: flex-start;
      }
      img {
        max-height: none;
      }
    }
  }
</style>
